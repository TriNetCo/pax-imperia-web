import FirebaseConnector from "./FirebaseConnector";

describe("FirebaseConnector tests", () => {

  test('FirebaseConnector can... render a test child that asserts ', () => {


    // FirebaseConnector

    // Stub initApp() to always return something that looks like a redirect signin result

    //
  });

});
