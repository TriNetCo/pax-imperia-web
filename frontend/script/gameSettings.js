export const GameSettings = {
    galaxyWidget: {
        canvasWidth: 800,
        canvasHeight: 400,
        systemCount: 100,
        systemRadius: 5,
        systemBuffer: 30,
        canvasBuffer: 15,
        maxPlacementAttempts: 30,

        minStarSize: 1,
        maxStarSize: 3,
        minPlanetCount: 1,
        maxPlanetCount: 5,
        minPlanetSize: 0.4,
        maxPlanetSize: 2,
    }
}
